#
# Let's Chat Built-in Defaults
#

server:
  port: 5000

xmpp:
  enable: true
  debug:
    handled: false
    unhandled: false
  port: 5222
  host: 'localhost'
  confhost: 'conference.localhost'

database:
  uri: mongodb://localhost/letschat
